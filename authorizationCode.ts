export async function authorizationCode(config: {
    authorizationUrl: string,
    accessTokenUrl: string,
    clientId: string,
    clientSecret: string,
    redirectUrl: string,
    scope: string,
}, debug?: boolean) {
    // TODO hard bruh
}
